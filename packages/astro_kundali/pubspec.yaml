name: astro_kundali
description: "Shared kundali rendering layer for the TalkAcharya customer and astrologer apps — models, the native chart painter, chart views, and the plain-language readings."
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  freezed_annotation: ^3.1.0
  intl: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.13
  freezed: ^3.1.0

flutter:
