name: talkacharya_predictions
description: "Shared paid-predictions layer for the TalkAcharya customer and astrologer apps — payload models, English label helpers, and the presentation widgets (cards, status chips, the delivered forecast, the astrologer brief). Networking, routing and DI stay in each app."
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  freezed_annotation: ^3.1.0
  intl: any

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  build_runner: ^2.4.13
  freezed: ^3.1.0

flutter:
